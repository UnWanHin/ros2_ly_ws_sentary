{
  "lastAnalyzedAt": "2026-07-20T09:00:00Z",
  "gitCommitHash": "HEAD",
  "workingTreeStatus": "Runtime source baseline is the current HEAD; this revision makes the offline simulator independent of a sentry.aim overlay while preserving the formal extended AimResult contract. An untracked LibreOffice temporary lock file is intentionally excluded from version control.",
  "lastCheckedNote": "Fallback graph source-checked against the current local runtime changes: JSON retains the complete Regional baseline, while AreaManager.yaml is the final live owner of the four MyArea and CommonCentral scope switches; EnemyArea remains JSON-owned. Tactical.yaml owns the final ProtectHero switch and ProtectCastle: Enable gates both castle sources, RFID gates only self_fortress_gain_point_status=2/3 plus stand-fire, and EnemyPos gates only enemy coordinates in MyBase; other RegionalDefense areas remain active. sentry_all gives navi_tf_bridge the same YAML-derived Chase scope. Safe fallback clears FireCode.FollowMode before FIELD_ALL publication, and Outpost manual goal mode suppresses bridge /goal_pose publication. DecisionTrace v4 records BT-authored tactical evidence plus the last actual control-output snapshot independently from lower-machine feedback; simulator v2/v3 adapters preserve missing evidence as not_recorded. Pygame and /tactical share SceneCatalog/SceneState; mock is the only simulator input owner and manual_ros is observer-only. MCAP export includes decision/control_output/tactical/scene channels. Offline launch neither sources nor requires sentry.aim: legacy four-field AimResult builds safely, but cannot authorize aim trajectory or fire; formal launch remains strict for the extended AimResult contract. Targeted offline launch tests passed. Static selfcheck remains host-dependent on ROS availability.",
  "version": "1.0.0",
  "analyzedFiles": 561,
  "generator": "local-understand-anything-compatible-fallback",
  "graphShape": {"nodes": 107, "edges": 145, "layers": 6}
}
