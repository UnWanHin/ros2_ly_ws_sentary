/**:
  ros__parameters:
    # Overlay for standalone navigation velocity tests.
    # Keep disabled in normal sentry/BT launches.
    io_config:
      navigation_test: true
      navigation_test_stale_timeout_ms: 500
